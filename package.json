{
  "name": "bdb-site",
  "version": "0.0.2",
  "description": "Build the BDB Static Site for bdb.semitics-archive.org",
  "main": "build.js",
  "repository": "git@github.com:jackweinbender/bdb-site.git",
  "author": "Jack Weinbender <jack.weinbender@gmail.com>",
  "license": "GPLv3",
  "scripts": {
    "start": "yarn run clean && yarn run build",
    "build": "mkdir dist && node build.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "handlebars": "^4.0.11"
  }
}
