{
  "name": "bdb-site",
  "version": "0.0.2",
  "description": "Build the BDB Static Site for bdb.semitics-archive.org",
  "main": "build.js",
  "repository": "git@github.com:jackweinbender/bdb-site.git",
  "author": "Jack Weinbender <jack.weinbender@gmail.com>",
  "license": "GPL-3.0",
  "scripts": {
    "start": "yarn run clean && yarn run build && yarn run serve",
    "setup": "mkdir dist",
    "build": "yarn run clean && yarn run setup && yarn run assets && yarn run images && node build.js",
    "clean": "rm -rf dist",
    "assets": "cp -r assets dist/assets",
    "get_img": "rm -rf images && mkdir images && python3 get_images.py",
    "images": "cp -r images dist/images",
    "serve": "php -S localhost:8000 -t dist/"
  },
  "dependencies": {
    "gm": "^1.23.1",
    "handlebars": "^4.0.11",
    "request": "^2.83.0"
  }
}
